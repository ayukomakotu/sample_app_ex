<%= form_with(url: search_users_path, scope: :search, local: true) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
    
    <%= f.submit "search user", class: "btn-btn-primary" %>
<% end %>
