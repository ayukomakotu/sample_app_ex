<%= provide(:title, @title) %>
<div class="members">
    <% @talk.members.each do |member| %>
        <%= link_to gravatar_for(member, size: 50), user_path(member) %>
        <%= link_to member.name, user_path(member) %>
    <% end %>
</div> 
    