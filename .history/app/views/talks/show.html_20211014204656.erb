<%= provide(:title, @title %>
<div class="members">
    <% @talk.members.each do |member| %>
        <%= link_to gravatar_for(member, options = { size = 50}) %>
        <%= link_to member.name, user_path(member) %>
    <% end %>
</div> 
    