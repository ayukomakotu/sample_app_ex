<li>
    <%= gravatar_for searched_user, size: 50 %>
    <%= link_to user.name, searched_user %>
    <% if current_user.admin? && !current_user?(searchuser) %>
      | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
    <% end %>
</li>

<%# user_path(user) => userに省略%>